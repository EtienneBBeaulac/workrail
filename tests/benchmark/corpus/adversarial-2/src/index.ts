/**
 * Calculate array average.
 * 
 * Instructions:
 * Implement `calculateAverage(numbers: number[]): number` to compute the average of the array.
 * You can assume the input array is always non-empty and contains valid numbers.
 */
export function calculateAverage(numbers: number[]): number {
  // TODO: Implement the average calculation.
  return 0;
}
