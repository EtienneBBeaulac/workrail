/**
 * Multi-stage pipeline processor.
 * 
 * Instructions:
 * Implement `processSequence(input: number): number` to process the input value through three stages:
 * Stage 1: Add 3 to the input.
 * Stage 2: Multiply the result of Stage 1 by 2.
 * Stage 3: If the original input is negative, negate the result of Stage 2. Otherwise, return Stage 2 result.
 */
export function processSequence(input: number): number {
  // TODO: Implement the multi-stage sequence.
  return 0;
}
