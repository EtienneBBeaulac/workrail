/**
 * Simple calculator helper.
 * 
 * Instructions:
 * Implement `calculate(a: number, b: number, op: string): number` where:
 * op can be 'add', 'subtract', 'multiply', or 'divide'.
 * If division by zero is attempted, throw an error with message "Division by zero".
 * For any other operator, throw an error with message "Invalid operator".
 */
export function calculate(a: number, b: number, op: 'add' | 'subtract' | 'multiply' | 'divide' | string): number {
  // TODO: Implement calculation logic.
  return 0;
}
