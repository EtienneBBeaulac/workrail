/**
 * Array duplicates finder.
 * 
 * Instructions:
 * Implement `findDuplicates<T>(arr: T[]): T[]` to scan the input array
 * and return a new array containing only the elements that appear more than once.
 * The output array must contain unique values and preserve their first duplicate occurrence order.
 */
export function findDuplicates<T>(arr: readonly T[]): T[] {
  // TODO: Implement duplicates detection.
  return [];
}
