// App.tsx is retained for reference but the application entry point is now
// main.tsx -> router.tsx -> AppShell. Navigation is handled by
// @tanstack/react-router with hash-based history so browser back/forward work.
//
// This file is intentionally left unused. It will be removed in a follow-up
// cleanup if no other references remain.
export {};
